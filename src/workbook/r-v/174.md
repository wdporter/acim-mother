---js
const prev = "/workbook/r-v/176"
const next = "/workbook/r-v/178"
const layout = "mylayout.njk"
const title = "Review (157 and 158)"
const volume = "W"
const chapter = "174"
const reference = `${volume}:${chapter}`
const headerTitle = `Lesson ${chapter}`
const navTitle = `${chapter}. ${title}`
const pageNumber = 4
const eleventyNavigation = {
	key: reference,
	order: pageNumber,
	title,
	parent: "W-rV",
	indexPageTitle: navTitle,
	breadcrumbTitle: navTitle,
	menuTitle: navTitle
}
---

**God is but Love, and therefore so am I.** {.text-center}

[157] Into His Presence would I enter now.

**God is but Love, and therefore so am I.** {.text-center}

[158] Today I learn to give as I receive.

**God is but Love, and therefore so am I.** {.text-center}
