---js
const prev = "/workbook/r-iv/149"
const next = "/workbook/121-140/121"
const layout = "mylayout.njk"
const title = "Review (139 and 140)"
const volume = "W"
const chapter = "150"
const reference = `${volume}-${chapter}`
const headerTitle = `Lesson ${chapter}`
const navTitle = `${chapter}. ${title}`
const pageNumber = 556
const eleventyNavigation = {
	key: reference,
	order: pageNumber,
	title,
	parent: "W-rIV",
	indexPageTitle: navTitle,
	breadcrumbTitle: navTitle,
	menuTitle: navTitle
}
---
### My mind holds only what I think with God.

(139) I will accept Atonement for myself.

(140) Only salvation can be said to cure. 

{% render "acim-org-link", pageNumber: pageNumber, reference: reference  %}