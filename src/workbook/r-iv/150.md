---js
const layout = "mylayout.njk"
const title = "My mind holds only what I think with God"
const reference = "W-150"
const superTitle = `Lesson ${reference.split("-")[1]}`
const pagenumber = 556
const eleventyNavigation = {
	key: reference,
	order: pagenumber,
	title,
	parent: "W-rIV"
}
---

(139) I will accept Atonement for myself.

(140) Only salvation can be said to cure. <acim-org-link pagenumber={{pagenumber}} reference={{reference}}></acim-org-link>