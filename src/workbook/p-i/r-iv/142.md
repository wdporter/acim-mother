---js
const title = "(123–124)"
const volume = "W"
const part = 1
const parent = `${volume}${part}:141-150`
const chapter = "142"
const reference = `${volume}${part}:${chapter}`
const headerTitle = `Lesson ${chapter}`
const navTitle = `${chapter}. ${title}`
const eleventyNavigation = {
	key: reference,
	order: chapter,
	title,
	parent,
	indexPageTitle: navTitle,
	breadcrumbTitle: navTitle,
	menuTitle: navTitle
}
const prev = "/workbook/p-i/r-iv/141"
const next = "/workbook/p-i/r-iv/143"
const layout = "mylayout.njk"
---
***My mind holds only what I think with God.***

[123] I thank my Mother for Her gifts to me.

[124] Let me remember I am one with God.
