---js
const title = "(157–158)"
const volume = "W"
const part = 1
const parent = `${volume}${part}:171-180`
const chapter = 174
const reference = `${volume}${part}:${chapter}`
const headerTitle = `Lesson ${chapter}`
const navTitle = `${chapter}. ${title}`
const eleventyNavigation = {
	key: reference,
	order: chapter,
	title,
	parent,
	indexPageTitle: navTitle,
	breadcrumbTitle: navTitle,
	menuTitle: navTitle
}
const prev = "/workbook/p-i/r-v/173"
const next = "/workbook/p-i/r-v/175"
const layout = "mylayout.njk"
---

**God is but Love, and therefore so am I.**

[157] Into His Presence would I enter now.

**God is but Love, and therefore so am I.**

[158] Today I learn to give as I receive.

**God is but Love, and therefore so am I.**
